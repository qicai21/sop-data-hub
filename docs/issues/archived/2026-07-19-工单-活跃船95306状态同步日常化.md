# 工单：活跃船 95306 状态同步日常化

- **日期**：2026-07-19
- **状态**：已完成
- **范围**：发现 active lot 的 (project, ship) → `ShipmentStatusSync` → 可选 closeout；launchd 每 2h

## 交付

| 路径 | 说明 |
| --- | --- |
| `src/sop_hub/sop/active_status_sync.py` | 发现活跃船 + 批量同步 + JSON 报告 |
| `scripts/sync_active_shipment_status.py` | CLI：`--dry-run`（默认）/ `--apply` / `--closeout` |
| `deploy/launchd/com.qicai21.sop-data-hub.status-sync.plist` | StartInterval 7200，`--apply --closeout` |
| `runtime/status_sync_reports/` | 每次运行报告；`*_latest.json` |
| `runtime/status_sync.log` | launchd 日志 |

## 行为

- 扫描 `dispatch_status ∈ pending_freight/enriched/loading/all_loaded/tracking/delivered`
- 每船跑箱级/车级 95306 状态回写（既有 `ShipmentStatusSync`）
- apply 后可选 `lifecycle_closeout`
- **不**重发 Excel、**不**重传门户、**不**写 95306 库

## 验证

```bash
PYTHONPATH=src .venv/bin/python -m pytest tests/test_active_status_sync.py -q
PYTHONPATH=src .venv/bin/python scripts/sync_active_shipment_status.py --dry-run
```

## 安装 launchd

```bash
cp deploy/launchd/com.qicai21.sop-data-hub.status-sync.plist ~/Library/LaunchAgents/
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.qicai21.sop-data-hub.status-sync.plist
# 或 kickstart 立即跑一次:
launchctl kickstart -k gui/$(id -u)/com.qicai21.sop-data-hub.status-sync
```
