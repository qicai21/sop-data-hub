# 工单:九三早间日对账合并与内部群文本回填

- 日期: 2026-07-13
- 类型: 定时任务治理 + 九三数据对账
- 状态: 已完成

## 目标

将原本分离的任务合并为每日 8:00 / 9:00 / 10:00 / 11:00 的“九三早间日对账”:

1. 读取九三晨报,确认当天快照已落库;
2. 执行 `95306 × 货票 Excel × 本地库` 三方核对;
3. 当天三方核对没有 Excel 源时只记录“源未到”,不得失败;
4. 当日完整成功一次后写成功标记,后续时点跳过;
5. 取消原 22:00 的单独九三货票对账任务。

## 同轮发现的问题

1. `jiusan_departure_text_reconcile` 不在 text-watch 的自动安全任务集合,导致 97 条 pending 从未执行。
2. 历史补建没有限定群名和有效文本,混入空文本、数据单发群、其他项目消息。
3. 散粮同步把“存在活动 lot02”当成全局前提;全部 lot02 已完结时,已由微信台账明确归属的
   历史票也会提前中止,无法补入对应批次。

## 改动

1. 新增早间日对账协调脚本与 DB 成功标记。
2. 仅内部群 `铁晟大豆业务内部沟通群` 的完整九三发车文本允许建该任务。
3. 将九三文本分票作为本地安全链自动执行。
4. 从 message_inbox 保存的该群微信聊天全量文本重建任务,旧 pending 标记为已替代。
5. 散粮同步改为逐票判断:命中微信台账的票可补入已完成批次;未知新票在无活动 lot02 时只告警,
   不写入历史船。
6. 用新的 launchd agent 替换旧的晨报/22:00 对账 agent;每日 8/9/10/11 触发,成功后由
   `scheduled_reconcile_run_log` 跳过同日后续时点。

## 验收

- 当天晨报快照与 Excel 对账均完成后仅成功一次;
- 无 Excel 的周日不阻塞任务;
- 内部群有效单船文本得到执行或明确人工复核;
- 非内部群/空文本不再生成九三发车文本任务。

## 执行结果

- 2026-07-13 已写入早间日对账成功标记;当天后续触发会跳过。晨报缺失时保持等待并在下一时点重试;
  货票 Excel 缺失仅记为核对结果,不作为失败。
- 微信内部群历史文本:共扫描 28 条,其中 24 条为有效发车文本;旧 pending 97 条全部替代处理,
  最终 5 条成功、96 条因非来源/不完整/需人工复核而跳过,无 pending。
- 95306 散粮票按微信台账重跑后:和谐1 320 车、诚信 431 车、美国 288 车,合计 1,039 车。
- 回归测试:35 passed。
- 历史回填脚本为一次性操作,执行验证后已清理;持续机制由来源守门、text-watch 和早间日对账承担。
