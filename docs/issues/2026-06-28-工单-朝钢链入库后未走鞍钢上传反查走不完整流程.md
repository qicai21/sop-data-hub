# 工单:朝钢检装车链 ingest/match 完成后,未走鞍钢门户上传+反查 → 流程走不完整(疑与昨天更新有关)

- **类型**:代码缺陷(链路未跑完 / 对外上传段未触发)
- **状态**:已处理;原问题为误报并已补发/加固,鞍钢上传审计可观测性另议
- **发现日期**:2026-06-28
- **发现会话**:数据运维(只改数据,不改代码)
- **处理归属**:系统开发会话
- **严重度**:高 —— 收货系统(鞍钢门户)收不到上传,业务未真正完结,但 dispatch_status 已置 completed(假完成)
- **用户判断**:就是不能走完整流程,**可能跟昨天(2026-06-27)的更新有关**,让开发去查。

## 现象

朝钢 马兰幸福 55车候选(`e0d89227…` / 源图 497 / wx_2026-06_2135),修好 inbox 回链后,verifier 13:18 `succeeded:1`:
- ✅ 55车已入 wagon_shipments(马兰幸福 lot1,标载/hph 齐)
- ✅ 候选 matched(reason `auto_matched_by_chain`)
- ✅ dispatch_status = **completed**
- ❌ **鞍钢门户上传 + 反查 没发生**:
  - `external_action_log` 这段时间无任何上传/反查动作(朝钢历史也只有 `send_shipping_excel_wechat`,从无上传动作类型)
  - 最新 chaoyang chain task 输出只到 `stage=95306_window_recover`,无 `consignee_upload`/`ansteel` 段
  - `wagon_shipments` 无 portal/上传痕迹

## 矛盾点

朝钢 lifecycle 明文(chaoyang.yaml line 15-20):**"发运 + 鞍钢门户上传 + 反查 OK 即完结"**。但本次**没上传/没反查就置了 completed** → 假完成,收货系统(鞍钢 56.ansteel)实际没收到这 55 车。

代码侧:`workflow_task_executor.py` line 1569-1653 有鞍钢上传+反查段;line 837/922/948/1576 有"合成候选 → skip_upload,不自动上传鞍钢,留人工确认"的分支。**需开发确认:verifier 重试路径(`_retry_chain` → `_execute_chaoyang_inspection_chain`)是否误走了 skip_upload 分支,或上传段根本没被触发。**

## 关联线索(疑同源)

- 6/19 中联发 44车 chaoyang_inspection_chain **failed**(task 362/361,"candidate missing fields")—— 同样没走完。
- 两次朝钢链都没走完整,**疑 2026-06-27 的更新动了朝钢链/verifier 重试路径**。请开发对照 6/27 的 commit 排查。

## 🔗 整合分析(2026-06-28,与"任务3"合并)

本工单(上传段 bug)与**任务3**(标载工单待办#1:`workflow_task_executor` 手搓 INSERT 收敛到 `ingest_wagons`)**在同一个函数 `_execute_chaoyang_inspection_chain` 里、相邻两段**,**建议一个朝钢专项 session 一起做**:
- 任务3 = ingest 段(line ~1169-1214,手搓 INSERT 重构);本工单 = 6c 上传段(line 1574+,假完成)。

**是否 6-27 改坏的——已部分排除**(开发会话核过):
- `3292572`(写hph/标载 INSERT):改在 1169-1214 ingest 段(正是任务3重构处),只加列、ingest 成功、**没动上传段**。
- `7e67bc2`(多批次excel):改在 1453-1571 6b发送段(紧贴上传前),但 `not _all_matched` 是 `skipped`+**继续往下、不是 return**(1514-1516),**6b 不拦 6c 上传 → 基本排除是它直接跳过上传**。

**真因聚焦**(6c 门控 = `if skip_upload:跳过 / elif chaoyang且loading_car_nos:上传`):
1. **大概率 `skip_upload=True`**:verifier 重试 `_retry_chain` → 链 `skip_upload=bool(synth_cand_id)`(line 926),把它当**合成候选**跳过上传(本是"留人工确认"设计)——但**不该同时把 dispatch_status 标 completed**(假完成)。
2. **completed 置位条件**:应"上传+反查 OK"才置,ingest 即置=假完成,违背 chaoyang.yaml lifecycle(line 15-20)。

**新 session 切入点**:先查 verifier 重试这次 `skip_upload` 是不是 True、为啥 True(马兰幸福是合成候选还是真通知单候选);再看 completed 门控;顺手把任务3 的 INSERT 收敛一起做。

## ✅ 结论(2026-06-28 系统开发会话查实):**误报 —— 上传+反查实际成功了**

用只读门户反查(login + query_wmwm01 + call_query_wmwm19,镜像 upload_and_verify 幂等闸前半段,**不上传**)实测:

```
target plan waybill: DL000762347
portal cars on this plan: 55
our DB cars (batch):     55
==> on portal: 55/55   MISSING: 0
```

**55 车全在鞍钢门户上,收货系统收到了,业务已完结。无需人工补传。**

工单三条"证据"全部是误判,逐条澄清:

1. **「external_action_log 无上传动作」** → 因为 `upload_and_verify` **从不写 external_action_log**(只 `send_shipping_excel_wechat` 走 plan/mark_external_action)。无记录 ≠ 没上传。**真问题:鞍钢上传/反查动作零审计痕迹**(见下"真实可改项")。
2. **「task 输出只到 stage=95306_window_recover」** → 那是**原文本/链 task**(挂 pending 的那次)。成功上传发生在 **verifier 13:18 retry** 里;而 `_retry_chain` 直接调函数、**不落 workflow_task**,所以成功那次没有 task 记录可看。
3. **「dispatch_status=completed = 假完成」** → 那是 `wagon_shipments.dispatch_status`,INSERT 时**恒为常量 'completed'**(line 1377,只表示"该车行已定稿入表"),**不是** batch 生命周期信号。真正的 `release_batches.dispatch_status` 此刻是 **`loading`(没 closed)** —— 不但不是假完成,反而是**没自动关批**(6d lifecycle close 没触发,疑 planned_tons 未设 / sw 未 ok)。

**两个原假设(skip_upload=True / 6b 早退)均排除**:`_retry_chain` 调链不传 skip_upload(默认 False,line 139-144),6b `not _all_matched` 是 skipped+继续不 return → 成功路径必达 6c,skip_upload=False+chaoyang+loading_car_nos 非空 → upload_and_verify **被调用且成功**。

## ✅ 后续(2026-06-28 晚,1→2→3 已做):用户发现"上传成功但发运excel没发"

排查:候选 05:18 已 matched、上传是 13:18 verifier 重试补的,**6b 发运excel 发送在两次之间丢了**(链 1462 match→1468 commit→1486 6b 顺序/可见性都正常,`_event_excel_batch_specs` 现返回 all_matched=True、生成正常、target=[GROUP013];说明是 13:18 重试路径没把 6b 跑完/没留痕——即 P2#3 的"重试丢 output"具体咬在 6b 上)。

- **#1 已补发**:复刻 6b(生成→plan→send→mark executed),马兰幸福 55车 发 [GROUP013] 成功,幂等键 `batches:6c23a1c991…:55` 已登记防重发。
- **#2 修两个 excel bug**(commit,`departure_excel.py`):① 文件名硬编码"中唐发运"→改按船名 `{船}发运_{发运日}_{N}车`;② 归档用 batch_date(马兰幸福=2026-08-27 脏数据)→改用车实际制票日(发运日),`_resolve_archive_dir` 加 date_override。
- **#3 根治假完成/6b丢失**(本提交,`workflow_task_executor.py`):链末尾**不再硬编码 `status:'succeeded'`**——鞍钢上传或发运excel 该成功却没成功 → `status='failed'`(verifier 重试,两者幂等不重复)+ `warnings` 字段暴露;合法跳过(已发/无批次/等齐/无配置)不算失败。**这同时落地了 P2#2(真假完成机制)**。

**遗留**:① 马兰幸福 `release_batches.batch_date='2026-08-27'` 脏数据,查建批日期解析为何把 6-27 弄成 8-27。② P2#1(upload_and_verify 不写 external_action_log)/ P2#3(verifier 透传 output)仍未做。

## 真实可改项(非工单原诉求,降级为加固;待用户拍板)

P2 观测性 / 潜在假完成,**本次没真出事**,但下次会咬人:

1. **观测性 [P2]**:`upload_and_verify` 不写 external_action_log → 朝钢上传+反查完全不可见/不可审计。应像 send_excel 一样 plan/mark,落 action_type=`upload_ansteel_consignee`。
2. **潜在真假完成 [P2]**:`_execute_chaoyang_inspection_chain` 末尾**硬编码 `"status":"succeeded"`(line 1639),完全不看 `consignee_upload.success`**。若上传真失败(login fail / missing 车),被 1601 try/except 吞掉、仍报 succeeded、verifier 记 succeeded:1 —— **这才是真正的假完成机制**(本次侥幸没踩)。应:chaoyang 上传失败时 status 降级 + 候选别置 matched-and-done / 发告警。
3. **观测性 [P2]**:verifier `_retry_chain` 丢弃链 output_json(含 consignee_upload),只留 succeeded 计数 → 成功/失败都看不到上传结果。应把 upload_info 透传进 VerifierSummary.detail。
4. **未自动关批 [P3]**:本批仍 `loading`,6d close 没触发,查 planned_tons / shipped_weight.ok。

## 待开发排查(原始,已被上面结论覆盖)

1. verifier `_retry_chain` 成功路径是否包含鞍钢上传+反查;还是只 ingest/match 就置 completed。
2. completed 的置位条件:是否应"上传+反查 OK"才置,而非 ingest 即置(避免假完成)。
3. 2026-06-27 更新是否改了朝钢链 step 顺序 / skip_upload 门控 / verifier 调用签名。

## 数据侧现状 / 临时

- 55车结算数据已齐(wagon_shipments)。但鞍钢门户**未上传** —— 需人工走标准上传+反查([[chaoyang-ansteel-upload-verify-rule]]:朝阳上传必须紧跟反查)。等用户确认后可手工补传。

关联工单:2026-06-28-候选未回填inbox链接、2026-06-28-共享群检装车单授权、2026-06-27-reconcile段逻辑。
