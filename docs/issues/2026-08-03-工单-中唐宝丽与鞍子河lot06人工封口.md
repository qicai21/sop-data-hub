# 工单：中唐宝丽与鞍子河 lot06 人工封口

## 用户确认

- 中唐特钢宝丽 `lot06` 已发运完成，设为 `all_loaded`。
- 中唐特钢鞍子河 `lot06` 已发运完成，设为 `all_loaded`。

## 执行边界

- 仅推进两个放货批次生命周期并同步关闭其 runtime 匹配规则。
- 不修改已入库车辆、不重新分票、不重发 Excel、不上传外部系统。

## 执行前核对

| 船名 | 批次 | 已入库车数 | 已发重量（吨） |
| --- | --- | ---: | ---: |
| 宝丽 | lot06 | 157 | 10,758.2 |
| 鞍子河 | lot06 | 149 | 10,183.2 |

## 验收

- 两条 `release_batches.dispatch_status` 均为 `all_loaded`。
- 对应 `release_dispatch_match_rules.status` 均为 `completed`，不再承接新发运匹配。

## 执行结果（2026-08-03）

- 已备份数据库：`data/backups/sop_agent_before_zt_baoli_anzi_lot06_all_loaded_20260803_162853.sqlite3`。
- 宝丽 lot06、鞍子河 lot06 均已推进为 `all_loaded`。
- 两条运行时匹配规则均已切换为 `completed`；本次未修改 306 条既有车辆记录，也未触发任何外部动作。
