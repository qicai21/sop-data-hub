# 工单：九三5号循环列调出

- **状态**：已完成
- **日期**：2026-07-21
- **业务生效日期**：2026-07-19

## 业务确认

5号循环列于2026-07-19返回锦州港后，车体已调走，不再参与九三大豆循环运输。

## 处理范围

1. `wagon_body_pool` 中九三5号列50车改为 `transferred_out`，记录 `transferred_at=2026-07-19`；
2. 看板将5号列显示为红色“调出”，不计入运行中循环车组；
3. 两小时95306轨迹任务停止抽查5号列；
4. 项目运营日志记录调出事实；
5. 固定测试保证看板和轨迹任务共用同一调出列名单。

## 验收

- [x] 5号列50车均为 `transferred_out`；
- [x] 循环看板仅统计1、2、3号共3列运行；
- [x] 轨迹采样不再查询4、5号列；
- [x] 测试、服务刷新和版本提交完成。

## 完成记录

- 数据库备份：`data/backups/sop_agent_before_jiusan_cycle5_transfer_20260721_153617.sqlite3`。
- `wagon_body_pool`：5号列50车均更新为 `transferred_out`，`transferred_at=2026-07-19`。
- 调出列名单统一为共享常量 `{4, 5}`，看板与轨迹采样不再各自维护。
- 真实刷新：3个运行中车组、12次95306查询、0错误；不再查询4号、5号列。
- 看板：标题显示“循环车组3列”，5号列保留“50车”历史信息并显示红色“调出”。
- 定向测试：27项通过。
