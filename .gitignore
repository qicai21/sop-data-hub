__pycache__/
*.pyc
*.pyo
.DS_Store
*.log
*.egg-info/
dist/
build/
.venv/
*.db

# 运行时数据
data/

# 大型样本图片（开发端使用，不入库）
业务群图片/

# 旧目录（迁移完成后保留在磁盘但不入库）
wechat_ops_agent/
业务Agent核心技能转移清单/
business-data-agent/

# IDE
.idea/
.vscode/

# 预览缓存
_previews/

# Dashboard runtime JSON (contains local absolute paths, not portable)
dashboard/dispatch_board_data.json

# 九三大豆看板 — 本地运行文件，不提交
dashboard/jiusan_dashboard_data.json

# Live service runtime — ephemeral output, not source
runtime/

# Generated departure excels — ephemeral output, not source
output/

# Generated departure reports — ephemeral output, not source
reports/

# 朝阳钢铁系统 auth bundle / 测试 response,不入仓
runtime/chaoyang_*.json
