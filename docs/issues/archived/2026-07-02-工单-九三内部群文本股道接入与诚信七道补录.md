# 工单: 九三内部群文本股道接入与诚信七道补录

状态：已处理

## 背景

1. 新增微信群 `铁晟大豆业务内部沟通群`，最新文本锚点为 `gdb8898`，需纳入监听。
2. 该群文本形态为 `八道 40节 新台子 大豆 诚信`，用于补齐每一列九三发车的真实作业道线。
3. 历史 `诚信` 数据与此前 `和谐1` 类似，散粮 `track` 与集装箱 `loading_line` 未落真实股道，用户确认可统一按 `七道` 补录。

## 目标

1. 锁定新群 `wxid`，并将其纳入九三 SOP 文本监听。
2. 让九三 departure text 识别支持 `新台子` → `jiusan`。
3. 将 `诚信` 历史散粮与集装箱线路统一补录为 `七道`。
4. 清理旧占位线路生成的 route C 历史费用批次，并重算费用与船级对账。
5. 修正 `bulk_loading_notice_wagon` 唯一键过窄问题，允许同日同线不同船并存。

## 实施

1. 从 WeChat `contact.db/message_0.db` 反查到:
   - 群名: `铁晟大豆业务内部沟通群`
   - wxid: `17885542384@chatroom`
   - 最新文本: `dongbei9234: gdb8898`
2. 在 `jiusan.yaml` 中新增 `GROUP094` 文本监听任务。
3. 补 `departure_text_parser` 与 `text_router` 的 `新台子` / `jiusan` 映射。
4. 将七道补录脚本泛化，可按 `--ship` 重跑。
5. 新增 `bulk_loading_notice_wagon` 唯一键迁移脚本:
   - 旧: `UNIQUE(project, notice_date, track, car_seq)`
   - 新: `UNIQUE(project, notice_date, track, car_seq, ship_name)`
6. 对 `诚信` 执行七道补录，并重算 route C / ship reconciliation。

## 验证

1. `八道 40节 新台子 大豆 诚信` 应解析为:
   - destination=`新台子`
   - project_id=`jiusan`
2. `wx_config.json` 中应包含新群 `17885542384@chatroom`。
3. `bulk_loading_notice_wagon.track` 与 `wagon_container_shipments.loading_line` 的 `诚信` 历史记录应统一为 `七道`。
