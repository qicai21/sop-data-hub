# 工单:中唐环球信任 33 车文本触发漏识别

- **类型**:功能缺陷 / 规则补齐
- **发现日期**:2026-07-02
- **状态**:已处理
- **严重度**:中

## 背景

用户反馈：中唐特钢项目今天清晨发出 `33` 车，但“没有触发流程”。

排查发现，本次业务事实其实已经由检装车通知单图片链先行入库：

- `release_batch_id=bb15b6b08ed309b2dc090e1699624eca544c981b`
- 船名：`环球信任`
- `wagon_shipments` 已新增 `33` 车
- 来源消息：`wx_2026-07_113`

但两条同义文本消息：

- `wx_2026-07_114`（铁晟业务工作群，`2026-07-02 01:43:09`）
- `wx_2026-07_5`（中唐特钢发运群，`2026-07-02 04:41:53`）

都被路由成了普通 `departure_flow / detect_departure_message`，且没有真正生成 workflow task。

## 根因

1. `环球信任` 未登记在 `config/project_sops/zhongtang.yaml` 的 `project_meta.known_ships`
2. 因此文本没有命中 `inspection_text_trigger_flow`
3. 又因为中唐没有普通 `departure_text` 的执行链，最终 `message_inbox.db_record_ids=[]`，文本空转

## 本工单范围

1. 在中唐 yaml 中补齐 `环球信任`
2. 同步补齐中唐文本项目推断关键词
3. 增加针对 `环球信任 33节` 的文本触发测试
4. 回填今天两条历史文本消息为正确路由并补建任务痕迹
5. 验证回填任务结果为“已由通知单链处理”

## 验收标准

- `煤五，汐子铁，环球信任，实装33节` 被识别为 `inspection_text_trigger`
- 历史两条消息不再停留在 `db_record_ids=[]`
- 回填任务执行后不重复入库、不重复发送，只标记“already_handled_by_notice_chain”

## 处理记录

已完成：

1. `config/project_sops/zhongtang.yaml`
   - `project_meta.known_ships` 新增 `环球信任`
2. `src/sop_hub/sop/text_router.py`
   - `_ZHONGTANG_SHIP_KEYWORDS` 新增 `环球信任`
   - `_project_default_dest()` 去掉对 `departure_excel/openpyxl` 的隐式依赖，改为直接扫 yaml
3. `src/sop_hub/sop/workflow_task_executor.py`
   - `inspection_text_trigger` 在“已 matched 候选”分支新增 24h fallback
   - 修复“通知单图晚于触发时窗落库时，文本触发器仍假 pending”
4. `tests/functional/test_inspection_text_trigger.py`
   - 新增 `环球信任 33节` 路由测试
   - 新增 `matched candidate outside event bounds` fallback 测试
5. 已回填历史消息：
   - `message_inbox.id=100002571` (`wx_2026-07_114`)
   - `message_inbox.id=100002581` (`wx_2026-07_5`)
6. 已补建并执行历史文本 trigger task：
   - `workflow_task_db.id=470`
   - `workflow_task_db.id=472`
   - 两条均收敛为 `already_handled_by_notice_chain`

## 业务核对

本次 `33` 车并非未入库。

实际业务事实已经由检装车通知单图片链完成：

- 候选：`inspection_ingestion_candidates.id=131cb31b4fe7dd9a820f1eea5b521ebace3dc1a9`
- 来源图片：`wx_2026-07_113`
- 候选抽取：`wagon_count=35`，但 footer 明确 `装车33 / 排车2`
- 已入库：`wagon_shipments` 新增 `33` 车
- `release_batch_id=bb15b6b08ed309b2dc090e1699624eca544c981b`（环球信任 lot08）

所以本工单修的是“文本触发漏识别/无任务痕迹”，不是补车。

## 验证

已验证：

- `.venv/bin/python -m pytest tests/functional/test_inspection_text_trigger.py -q`
  - `13 passed`
- `message_inbox`
  - 两条 `33节` 文本均为 `inspection_text_trigger_flow / inspection_text_trigger`
- `workflow_task_db`
  - task `470 / 472` 均为 `status=skipped`
  - `output_json.stage=already_handled_by_notice_chain`
