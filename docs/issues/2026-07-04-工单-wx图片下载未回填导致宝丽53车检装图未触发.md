# 工单: wx 图片下载未回填导致宝丽 53 车检装图未触发

状态：待处理

## 背景

用户反馈：

> 中唐特钢 宝丽 lot1，有 53 车 15.48 发的消息，没触发流程，优先确认船名的问题。

排查后确认：本次不是船名识别问题，而是图片下载/回填链路没有把检装车通知单图片落地，导致 SOP 图片识别链无法建候选。

## 当前业务影响

- 项目：`zhongtang_special_steel`
- 船名：`宝丽`
- lot：`lot01`
- 文本消息：`煤四 汐子铁 宝丽 实装53节`
- 中唐群消息：
  - 文本：`message_inbox.id=100002872`, `message_id=wx_2026-07_18`, `received_datetime=2026-07-04 15:43:38`
  - 图片：`message_inbox.id=100002873`, `message_id=wx_2026-07_19`, `received_datetime=2026-07-04 15:43:39`
- 铁晟群副本：
  - 文本：`message_inbox.id=100002871`, `message_id=wx_2026-07_360`, `received_datetime=2026-07-04 15:40:16`
  - 图前后也有多个 `waiting_media` 图片

## 已确认不是船名问题

1. `config/project_sops/zhongtang.yaml` 的 `known_ships` 已包含 `宝丽`。
2. `宝丽 lot01` release_batch 已存在：
   - `release_batch_id = 942a5aec6b12c47cf96d12ce02f31d506ceaaeaf`
   - `project = zhongtang_special_steel`
   - `ship_name = 宝丽`
   - `batch_sequence = lot01`
   - `dispatch_status = enriched`
   - `plan_id = 90260700001`
   - `contract_no = ZLZT-2026070101`
3. `release_dispatch_match_rules` 已存在 active 规则：
   - `ship_name = 宝丽`
   - `destination_station = 汐子`
   - `cargo_name = 铁矿`
   - `status = active`
   - `matching_tokens_json` 包含 `宝丽 / 汐子 / 铁矿 / lot01`
4. 文本触发任务已正确解析：
   - task `520`: `wx_2026-07_18`
   - task `519`: `wx_2026-07_360`
   - `trigger_project = zhongtang_special_steel`
   - `trigger_ship = 宝丽`
   - `trigger_dest = 汐子`
   - `trigger_expected_count = 53`

## 当前卡点

`workflow_task_db` 中两个文本触发任务仍为 `pending`：

- `519`
- `520`

输出阶段均为：

```json
{
  "stage": "waiting_inspection_notice",
  "ship": "宝丽",
  "destination": "汐子",
  "expected_count": 53,
  "note": "检装车通知单图未到,保持 pending 等下一轮"
}
```

但 `message_inbox.processing_status` 显示为 `task_failed`，对业务看板/排障有误导性：底层任务实际仍是 pending 等图，不是业务处理已失败。

## 图片下载证据

wx-ops-agent 原始 ledger：

```json
{"seq": 18, "time": "2026-07-04 15:43:38", "msg-type": "text", "msg-content": "煤四 汐子铁 宝丽 实装53节"}
{"seq": 19, "time": "2026-07-04 15:43:39", "msg-type": "image", "msg-content": "", "msg-path": "未下载"}
```

对应图片下载 todo：

```json
{
  "seq": 19,
  "time": "2026-07-04 15:43:39",
  "msg-type": "image",
  "msg-path": "未下载",
  "image_md5": "37f02026a62ce7edc2724ed131a2a753",
  "image_key": "37f02026a62ce7edc2724ed131a2a753",
  "local_id": 26,
  "todo_key": "26:37f02026a62ce7edc2724ed131a2a753"
}
```

截至本工单创建时，`image_download_todos/2026-07.jsonl` 中没有对应 `resolved` 行。

## 当前 pending 范围

按 todo 文件统计：

- `中唐特钢发运群`: pending 图片 1 条
  - 即 `26:37f02026a62ce7edc2724ed131a2a753`
- `铁晟业务工作群`: pending 图片 22 条
- `铁晟大豆业务内部沟通群`: pending 图片 4 条

这说明问题可能不只是某条中唐消息，而是近期 UI 拉图或图片回填链路整体存在失效/未执行。

## 需要继续确认的问题

1. wx-ops-agent 当前 daemon 是否实际执行了 `pullimage-cycle`。
2. 若执行了，是否打开了正确群：
   - 中唐特钢发运群应使用 `[GROUP003]`
3. 若 UI 已拉图，为什么 hardlink / image resolver 没把 `image_key=37f02026a62ce7edc2724ed131a2a753` 回填成真实图片路径。
4. 当前运行进程的日志实际写到哪里；`data/runtime-logs/daemon-auto.log` 看到的是旧日志，不包含 2026-07-04 当前运行记录。
5. `message_inbox.processing_status=task_failed` 是否应在底层任务仍 pending 时改为更准确的状态，例如 `task_pending` / `waiting_media`，避免误判。

## 建议处理顺序

1. 先处理 wx-ops-agent 图片下载/回填：
   - 对 `中唐特钢发运群 seq=19 / local_id=26 / image_key=37f02026a62ce7edc2724ed131a2a753` 触发拉图或重新解析。
   - 成功后确认 ledger 出现 resolved 行。
   - 确认 `message_inbox.raw_standard_image_path` 被回填。
2. 再让 sop-data-hub 的 live_service 继续处理 pending task `519/520`。
3. 若能够入库并生成发运 Excel，再检查：
   - `wagon_shipments` 是否写入 53 车
   - 发运 Excel 是否生成
   - 微信发送是否成功
4. 最后修状态显示：
   - 文本触发器等待图片时，不应把 `message_inbox.processing_status` 暴露成 `task_failed`。

## 验收标准

- 宝丽 53 车检装车通知单图片可在 `message_inbox.raw_standard_image_path` 中找到真实文件路径。
- `inspection_ingestion_candidates` 为宝丽 lot01 建出候选，且匹配 `release_batch_id=942a5aec6b12c47cf96d12ce02f31d506ceaaeaf`。
- `wagon_shipments` 入库 53 车。
- 发运 Excel 正常生成并发送。
- 对应文本任务 `519/520` 不再停留在 `waiting_inspection_notice`。
- 后续类似“文本已到、图片未下载”的状态在看板上不再显示为误导性的 `task_failed`。
