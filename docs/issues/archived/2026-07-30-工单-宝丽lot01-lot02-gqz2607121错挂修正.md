# 工单：宝丽 lot01/lot02 `gqz2607121` 错挂修正

- 日期：2026-07-30
- 项目：中唐特钢
- 状态：已完成

## 现象

查询计划号 `90260700027`（宝丽 lot02）时，数据库显示包含
`gqz2607121` 的 53 车。但已归档并实际发送到数据单发群的 Excel：

`.../宝丽/lot01/excel/2026-07-12/宝丽发运_2026-07-12_53车.xlsx`

明确标注计划号 `90260700001`，车号集合与 `gqz2607121` 完全一致。

## 已确认事实

- `gqz2607121`：53 车，2026-07-12 09:35-09:39，煤四，应归宝丽 lot01 /
  `90260700001`。
- 当前错误归属：宝丽 lot02 / `90260700027`。
- 因此当前 lot01 少 53 车（144 → 197），lot02 多 53 车（290 → 237）。

## 修正范围

1. 将该列 53 条 `wagon_shipments.batch_id` 改为 lot01。
2. 同步本地 `shipment_release_batch_matches.release_batch_id`。
3. 对两个批次重算发运重量、剩余量、`actual_wagon_count`。
4. 落入 `reconcile_action_log`，保留逐车 `ydid` 的迁移审计。
5. 不重发 Excel、不触发微信或门户动作。

## 验收标准

- `gqz2607121` 全部 53 个 ydid 归 lot01，lot02 不再包含。
- lot01 为 197 车，lot02 为 237 车；两批次的聚合字段与 wagon 事实一致。
- 原 7 月 12 日 Excel 保持不变，外部动作不重复执行。

## 处理结果

- 修正前已备份：
  `runtime/backups/sop_agent_before_baoli_gqz2607121_reroute_20260730_1038.sqlite3`。
- `gqz2607121` 的 53 个唯一 `ydid` 已从宝丽 lot02 迁至 lot01；本地
  `shipment_release_batch_matches` 的 53 条关联同步迁移。
- 每个 `ydid` 写入 `reconcile_action_log`，来源标记为
  `2026-07-30/宝丽发运Excel/90260700001`。
- 重算后：
  - lot01 / `90260700001`：197 车，发运重量 13,509.2 吨；
  - lot02 / `90260700027`：237 车，发运重量 16,363.8 吨。
- 未创建新的外部动作记录，未重发 Excel、微信或门户上传。

## 提交

- 待补充。
