# 工单：微信停机后中唐鞍子河三列漏入库与候选目的地未回填

- 日期：2026-07-26
- 状态：已完成
- 范围：中唐特钢、微信图片补拉、检装车候选 rendezvous、95306 对账

## 现象

以 2026-07-24 21:55 宝丽 51 车为起点核对后发现：

1. 宝丽 7 月 24 日晚 51 车、7 月 25 日上午 51 车已完整进入
   `wagon_shipments`。
2. 微信升级停机期间，鞍子河后续三列未进入 `wagon_shipments`：
   - 7 月 25 日晚混装通知单中的 13 车，作业线路煤五；
   - 7 月 25 日 23:00 通知单 48 车，作业线路煤四；
   - 7 月 26 日 03:06 通知单实装 53 车、排车 2 车，作业线路煤五。
3. 95306 已存在对应制票事实：
   - 7 月 25 日 21:29 共 61 票，等于 13 + 48；
   - 7 月 26 日 03:02 共 53 票。
4. 48 车图片补拉并识别后，候选已识别船名、项目和 release batch，
   但没有把相邻文本中的目的地“汐子”回填到候选，导致
   `candidate_missing_ship_dest_project / pending_review`，标准链路未继续。

## 处理要求

1. 恢复停机期间的检装车通知单图片，并以通知单分列、95306 车号和制票事实
   交叉核验。
2. 将 13、48、53 车分别按真实事件写入鞍子河 lot06，保留各自作业线路和
   列车编组号。
3. 生成并发送各列发运 Excel，清理关联 pending task / pending review。
4. 修复候选与文本 rendezvous：候选已有唯一 ship/project/release batch，
   且相邻可信文本给出唯一 destination 时，应补齐目的地后继续标准链。
5. 增加功能测试，提交 git，重启受影响服务并做业务结果核验。

## 处理结果

1. 微信 4.1.9 恢复后执行 `pullimage-cycle`，停机期间图片已补拉；鞍子河
   48 车、53 车通知单恢复成功。
2. 修复候选与文本会合的两个边界：
   - 候选已有正确船名/项目、只缺目的地时，可信相邻文本可补齐目的地；
   - 延迟补拉以微信原始 `received_datetime` 对事件窗口，不再使用补处理时
     才生成的 candidate `created_at`。
3. 中唐同一 95306 制票窗口存在两列时，增加“通知单精确子集”规则：
   通知单实装数已齐且全部在 95306 命中时，按通知单车集分列，不再把同窗
   另一列误判为漏车。该规则仅对中唐启用，不改变朝钢不拼列规则。
4. 鞍子河 lot06 已补齐三列：
   - `gqz2607252`：13 车，煤五，912.6 吨；
   - `gqz2607253`：48 车，煤四，3243.2 吨；
   - `gqz2607261`：53 车，煤五，3593.2 吨。
5. 三列合计 114 车、7749.0 吨，全部有唯一 ydid，`unresolved_wagon_count=0`；
   lot06 剩余 2306.0 吨，状态 `loading`。
6. 三份发运 Excel 均已发送至数据单发群：
   - `鞍子河发运_2026-07-25_13车.xlsx`
   - `鞍子河发运_2026-07-25_48车.xlsx`
   - `鞍子河发运_2026-07-26_53车.xlsx`
7. 补偿处理产生的 53 车重复候选已收口为 `superseded`，真实图片候选保留为
   `matched`；关联文本任务均结束。
8. 宝丽 lot 分配复核：
   - 7 月 24 日晚 51 车全部归 lot05；
   - 7 月 25 日上午 51 车按通知单顺序前 18 车归 lot05、后 33 车归 lot06；
   - lot05 最终 147 车、10032.4 吨，状态 `all_loaded`；
   - lot06 当前 33 车、2265.3 吨，状态 `loading`。
9. 数据库备份：
   `data/backups/sop_agent_20260726_before_anzihe_3trains_recovery_0920.sqlite3`。
10. 回归测试：
    `39 passed`。
