# 工单：中唐中联发 / 优雅首铁路放货建批

- **日期**：2026-08-31
- **状态**：已完成
- **范围**：中唐特钢，高桥镇→汐子；中联发 lot01

## 现象

中联发的出港计划通知单已被识别为中唐项目，但到港船名未登记在中唐 `known_ships`，因此创建批次被白名单闸拒绝。补白名单后复核发现该单没有 `remarks` 明细，全部铁路/到站信息只在“特约事项”，现有建批器将其错误地过滤为 0 批次。中唐特钢发运群已补充进口大船“优雅”及货运信息。

## 权威来源与口径

- 放货通知单：铁晟业务工作群 `wx_2026-08_2750`，2026-08-31 09:43；中联发、汐子、铁路、17,386 吨。
- 货运信息：中唐特钢发运群 `wx_2026-08_201`，2026-08-31 09:42；优雅、混合粉、17,500 吨、计划号 `90260800106`、合同号 `HT-ZT-20260826`。
- 到港船名：中联发；进口大船名：优雅。批次控制量以放货通知单 17,386 吨为准；货运信息 17,500 吨保留为合同货运字段，不覆盖放货量。

## 修复

1. 将中联发加入中唐 `known_ships`，仅授权中唐汐子铁路流向。
2. 修复单批次通知单无 `remarks` 时的兜底：以“特约事项”构造 `lot01`，数量仍取通知单货物表总重；到站归一支持“沙子站”→“汐子”。
3. 修复历史批次 `source_json` 非 JSON 时全量匹配规则刷新中断的问题，读取端降级为空对象，不阻断其它受管批次。
4. 从原始出港通知单创建唯一的中联发 lot01。
5. 将进口大船、货物品名、计划号及合同号补入该批次，关联两条原始消息。
6. 添加 YAML 白名单、无 remarks 单批次及历史坏 JSON 回归测试；本次无 95306 制票，不生成 Excel、不发送微信。

## 验收

- 批次为中唐 `中联发 lot01`，到港船名中联发、进口船名优雅、货物品名混合粉。
- 放货批次重量 17,386 吨，计划号及合同号准确；状态为 `enriched`，匹配规则为 active。
- 受管船名回归测试通过。

## 执行结果（2026-08-31）

- 已建立唯一批次 `f4e5adae4d08a6cf94fc1a29e5e421533289375e`：中联发 lot01，高桥镇→汐子，放货控制量 17,386 吨。
- 已补全：进口船名优雅、货物品名混合粉、计划号 `90260800106`、合同号 `HT-ZT-20260826`；合同货运数量 17,500 吨保留在 `quantity_tons`，未覆盖放货控制量。
- 已关联原始消息：放货通知单 `wx_2026-08_2750` 与中唐货运信息 `wx_2026-08_201`；批次状态为 `enriched`，汐子/铁矿匹配规则为 `active`、优先级 100。
- 写库前备份：`data/backups/sop_agent_before_zt_zhonglianfa_youya_lot01_apply_20260831_102528.sqlite3`。
- 回归：`make test` 706 passed；`make smoke-runtime` OK。
- 本批次尚无铁路车票，本次未生成或发送发运 Excel，也未调用任何外部上传。
