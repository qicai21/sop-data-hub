# 工单：局域网 Web 看板接入真实数据并取消登录

- **日期**：2026-07-23
- **状态**：已完成
- **前置工单**：`2026-07-23-工单-局域网只读货运Web看板.md`

## 背景

第一阶段静态页面已由 Windows 在局域网内验证可访问。现在进入第二阶段：

- 删除用户名、密码和 Basic Authentication；
- 删除 `runtime/dashboard_web_credentials.json`；
- 页面接入现有货运看板；
- 保持局域网 `10.1.2.0/24` 限制；
- 保持只读，不提供任何业务写入或链路执行接口。

## 实现口径

- Web 服务复用 `scripts/cli_dashboard.py` 的查询与文本渲染；
- Web 请求必须调用只读渲染模式，不能触发 `refresh_active_shipped_weights()`；
- ANSI 状态颜色转换为 HTML；
- 页面每 5 秒请求一次 `/api/dashboard`；
- 服务端对渲染结果做短时缓存，避免多人同时刷新重复查询数据库；
- `/healthz` 用于服务检查。

## 验收

1. Windows 直接打开 `http://10.1.2.193:8765`，不出现登录框。
2. 页面显示四个 SOP 项目、承认车、批次、挂起汇总和服务状态。
3. `/api/dashboard` 返回真实只读看板快照。
4. 非局域网地址仍返回 `403`。
5. 凭据文件和 plist 中的凭据参数全部删除。
6. 测试覆盖自动刷新、颜色转换、API 和只读渲染。

## 完成情况

- 已删除 Basic Authentication、用户名和密码配置；
- 已删除运行期凭据文件 `runtime/dashboard_web_credentials.json`；
- 已接入现有货运看板真实数据，页面每 5 秒自动刷新；
- Web 渲染使用只读模式，不刷新重量、不执行补录或业务链路；
- 已隐藏本机文件路径和终端控制提示；
- 保留 `10.1.2.0/24` 局域网访问限制；
- 已重载 `dashboard-web`，并同步重启终端 `board` 看板；
- 浏览器实测 `1280x720` 无横向溢出，页面可正常显示四个 SOP 项目、
  承认车、批次、挂起汇总、九三循环和服务状态；
- 回归测试：`37 passed`。
