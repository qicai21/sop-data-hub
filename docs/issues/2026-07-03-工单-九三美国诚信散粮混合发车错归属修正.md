# 工单:九三美国/诚信散粮混合发车错归属修正

- **状态**：部分完成 — 主修正已处理；诚信批次状态残留另议
## 背景

2026-07-03 早上新增九三大豆 `美国` 轮放货批次前,铁晟大豆业务内部沟通群出现混合散粮车发车文本:

```text
七道 新台子 大豆 诚信  5节，45节 美国  K车
```

文本含义:
- 本次共 50 车 K 车散粮车。
- 其中 `8101357, 8107032, 8101089, 8100992, 8100114` 共 5 车属于 `诚信 lot2`。
- 其余 45 车属于 `美国 lot2`。

## 问题

当时系统尚未添加 `美国` 船名/放货批次,九三散粮 95306 同步脚本对未命中通知单台账的新票使用兜底船。2026-07-03 02:07-02:09 的 50 张散粮票被临时落到 `和谐1 lot2`,形成错归属。

## 处理口径

1. 将该文本作为九三散粮车发车通知单台账补录来源。
2. 按用户确认车号拆分:
   - 5 车:归属 `诚信 lot2`
   - 45 车:归属 `美国 lot2`
3. 以 `ydid -> ship_name` 补齐 `bulk_loading_notice_wagon` 台账。
4. 重新运行 `scripts/sync_jiusan_bulk_wagons.py --apply`,由现有同步逻辑按台账重路由 `wagon_shipments`。

## 验收

- [x] `wagon_shipments` 中 2026-07-03 02:07-02:09 这 50 张散粮票不再归属 `和谐1 lot2`。
- [x] 指定 5 车归属 `诚信 lot2`。
- [x] 其余 45 车归属 `美国 lot2`。
- [x] 重跑九三散粮同步后归属不回跳。

## 处理结果(2026-07-03)

- 已补录 `bulk_loading_notice_wagon` 50 条:
  - `诚信`:5 条
  - `美国`:45 条
- 已运行 `scripts/sync_jiusan_bulk_wagons.py --apply`:
  - `重路由 50`
  - `美国 lot02`:45 车,装车重量 2825.0 吨,剩余 15175.0 吨
  - `诚信 lot02`:431 车,其中 426 车已交付、5 车仍为 loading
- 已将 `美国 lot02` release batch 从 `enriched` 推进到 `loading`。
- 已对原消息任务 `workflow_task_db.id=486` 执行现有 executor,结果为 `skipped/not_implemented`,避免继续 pending。
- 再次 dry-run `scripts/sync_jiusan_bulk_wagons.py` 验证:
  - `重路由 0`
  - `美国`:45 票,全部命中通知单台账
  - `诚信`:431 票,全部命中通知单台账

## 残留观察

`诚信 lot02` 批次头仍为 `confirmed_received`,但其中有本次补入的 5 张 2026-07-03 票为 `loading`。状态机不允许 `confirmed_received -> loading` 自动回退,因此本次未强制重开诚信批次;如看板需要展示诚信尾车,应单独按人工重开批次处理。
