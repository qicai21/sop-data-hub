# 工单: 九三 95306 实时轨迹切换综合账号

## 背景

`rail95306-sync` 从原五账号迁移为“锦州港综合账号 + 保留 tsz”的生产计划。
九三循环列实时四探针查询仍在代码和项目 YAML 中写死 `xts`。如果停止
`xts` 的生产维护而不迁移，该两小时轨迹任务会因旧 ticket 失效而中断。

## 根因

- `src/sop_hub/sop/jiusan_cycle_tracking.py` 的 `DEFAULT_ACCOUNT = "xts"`；
- `config/project_sops/jiusan.yaml` 的 `rail95306.account = "xts"`；
- 项目说明仍将 `xts` 描述为九三 95306 事实来源。

## 改动

1. 九三实时四探针查询默认账号切换为 `jz-port-super`。
2. 项目 YAML 及项目说明同步到综合账号口径。
3. 增加回归测试，固定默认账号与项目 YAML 一致。

## 验收

- 四探针更新报告记录 `account=jz-port-super`；
- 新账号能够按 `ydid` 查询高桥镇到新台子货票；
- 两小时更新任务完成且不增加查询错误；
- 不改变循环列选车、节点判断和缓存格式。

## 状态

- 代码、YAML、项目说明及回归测试已完成。
- 针对性测试：`28 passed`。
- 真实四探针查询：3 列、12 次查询、`errors=0`，缓存报告记录
  `account=jz-port-super`。
- 待执行：提交；生产 worker 切换后复核两小时任务。
