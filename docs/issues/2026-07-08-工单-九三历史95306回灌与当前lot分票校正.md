# 工单: 九三历史 95306 回灌与当前 lot 分票校正

## 目标

按用户确认口径推进九三大豆补数据:

1. 先让 95306 与 Excel 货票总数对齐。
2. 95306 因 2026 年初才开始覆盖,缺的早期部分不追。
3. 在 95306 对齐后,把明细回灌到 `wagon_shipments` / `wagon_container_shipments`。
4. 最后完成当前在途批次的分票校正。

## 已核对的口径

- `里奥格兰德 lot01 / lot02`
- `阿里斯托 lot01`
- `昆娜 lot01`
- `和谐1 lot01 / lot02`
- `诚信 lot01 / lot02`
- `美国 lot01 / lot02`

上述 Excel 货票号与 95306 均可逐票命中。

## 当前确认的缺口

- `意大利 lot01`:
  - Excel 共 1182 票有效货票号
  - 95306 可命中 646 票
  - 未命中 536 票中,`436` 票为 2025 年 12 月历史段,`100` 票为 2026-01-01 早段缺口
  - 按用户口径,这部分 95306 缺数先不追
- 本地 `wagon_*`:
  - `里奥格兰德 lot01 / lot02` 为空
  - `阿里斯托 lot01` 为空
  - `意大利 lot01` 为空
  - `诚信 lot01` 比应有少 1 箱
  - `美国 lot01` 比应有多 1 箱

## 历史重叠票处理

历史文件之间存在 14 票重叠:

- `意大利.xlsx` 与 `阿里斯托.xlsx` 重叠 4 票
- `里奥格兰德.xlsx` 与 `阿里斯托.xlsx` 重叠 10 票

处理原则:

- `阿里斯托.xlsx` 各 sheet 带显式船名列,优先级高于 `意大利.xlsx / 里奥格兰德.xlsx` 这类标准货票表。
- 因此重叠票统一归 `阿里斯托 lot01`。
- 最终本地有效票数按**去重后**落库:
  - `意大利 lot01 = 642 票 / 1284 箱`
  - `里奥格兰德 lot01 = 840 票 / 1680 箱`
  - `阿里斯托 lot01 = 1198 票 / 2396 箱`

## 本次处理结果

- 历史 95306 回灌完成:
  - `意大利 lot01` 回灌 642 票 / 1284 箱
  - `里奥格兰德 lot01` 回灌 840 票 / 1680 箱
  - `里奥格兰德 lot02` 回灌 235 票
  - `阿里斯托 lot01` 回灌 1198 票 / 2396 箱
- 当前 lot01 分票校正完成:
  - `诚信 lot01 = 766 票 / 1529 箱`
  - `美国 lot01 = 141 票 / 280 箱`
- `wagon_*` 明细中的 `hph / waybill_no` 已回填,后续分票可直接按货票号核对。

## 处理方案

1. 新增一次性脚本,按 Excel 货票号从 95306 回灌历史明细到本地两张 wagon 表。
2. 同步回填九三现有 wagon 明细里的 `hph / waybill_no`。
3. 用现有 `reconcile_jiusan_containers.py` 按用户确认的 2 张混票例外修正:
   - `GZDJT0249088:1507280 -> 美国`
   - `GZDJW0501693:3390802 -> 美国`

## 验收

- 历史三船和里奥格兰德散粮在本地 wagon 明细落库。
- `诚信 lot01 = 766 票 / 1529 箱`
- `美国 lot01 = 141 票 / 280 箱`
- 缺失的意大利早期 95306 段仅保留在缺口报告中,不误造数据。
