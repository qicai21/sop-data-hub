# 工单：箱级表 95306 状态回写（wagon_container_shipments）

- **提出日期**：2026-07-19
- **状态**：已完成
- **来源**：all_loaded 停车场后续；吉林 closeout 只认箱级事实源
- **范围**：`ShipmentStatusSync` 读 95306 → 写 sop 车级**与**箱级；**不**重发 Excel、**不**重传门户、**不**写 95306

## 现象

- 吉林 all_loaded 批箱级 `latest_stage_key` 大量为空 → closeout 永不能 all_received。
- 原 sync 仅 load/update `wagon_shipments`。

## 修复

1. `_load_units`：加载 wagon + container；吉林 project 只同步箱级。
2. 匹配优先 **ydid** → car+到站 → car。
3. 回写 `status_name`、`latest_stage_key`（交付/确认收货/已卸车映射）+ 时间字段（空才填）。
4. **不再**把 release_batches 强行改成 `dispatch_status=delivered`（交由 closeout 推 confirmed_received）。
5. 测试：`test_container_sync_by_ydid_writes_stage_and_status`。

## 现网执行（本工单）

- 备份：`data/backups/sop_agent_before_container_status_sync_20260719_141234.sqlite3`
- apply：`马兰希望` + `蓝鳍`（jilin）
- closeout 随后推进：
  - 马兰希望 lot02、lot04 → **confirmed_received**
  - lot05 仍 all_loaded（交付未满 234/308）
  - 蓝鳍 lot07/08 held_missing_loading_line（缺装车道线）
- text-watch：`launchctl kickstart -k …text-watch`

## 验证

```bash
PYTHONPATH=src .venv/bin/python -m pytest tests/functional/test_shipment_status_sync.py -q
# 11 passed
```

## 结案

- 代码：`src/sop_hub/sop/shipment_status_sync.py`、`scripts/sync_shipment_status_from_95306.py`
- 测试：箱级 ydid 路径
- 归档：`docs/issues/archived/`
