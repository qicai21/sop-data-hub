# 需求(低优先,排后):wx-ops-agent 媒体下载卡死(waiting_media)需重试

- **提出日期**:2026-06-23
- **优先级**:低(往后排;有合成兜底+幂等闸,结果不受影响,不紧急)
- **类型**:健壮性

## 背景
2026-06-23 朝阳中联发事故复盘时发现:03:18 的中联发检装车通知单图(message wx_1585)
**media_status 一直卡在 `waiting_media`(媒体没下载下来)** → 没分类 → 没生成候选 →
发车文本触发器找不到新候选 →(老代码)回退抓旧候选。

虽然 e30a067 三道闸 + 95306 合成兜底 + 鞍钢幂等闸已让"图没到也能正确入库",但
**根上的"图到了却没下下来"没修**:wx-ops-agent 抓到消息、登记了 image,但媒体拉取
失败后没有重试,就永久 waiting_media。

## 期望
1. 排查 wx-ops-agent 媒体下载失败原因(微信端媒体过期?网络?并发?)。
2. 加**重试机制**:waiting_media 超 N 分钟未就绪 → 重新拉取;多次失败告警。
3. (可选)检验类项目媒体长期拉不到 → 主动触发 95306 合成兜底(现为被动等宽限期)。

## 关联
- 事故复盘见记忆 [[inspection-text-trigger-arch]](2026-06-23 rendezvous 退化)
- 兜底链路:Fix C 95306 合成(`inspection_95306_synthesize`)、鞍钢幂等闸(upload_and_verify)
