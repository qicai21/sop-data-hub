# 工单:邮件查询机器人下线

- **日期**:2026-07-21
- **类型**:功能下线
- **状态**:已完成
- **关联工单**:`docs/issues/archived/2026-06-22-邮件查询机器人.md`

## 决定

用户确认邮件查询功能不再需要，停止通过 Mail.app 接收查询并自动生成邮件回复。

## 处理

1. 删除实现脚本 `scripts/email_query_bot.py`。
2. 卸载并删除 LaunchAgent `com.qicai21.sop-data-hub.email-bot`，避免登录或重启后恢复运行。
3. 保留 `runtime/email_query_bot.log` 和 `runtime/email_query_bot_state.json` 作为历史审计记录，不再写入。
4. 检查仓库引用，确认没有其他 SOP 流程依赖该脚本。

## 验证

- `launchctl` 中不存在 `com.qicai21.sop-data-hub.email-bot`。
- 系统中不存在 `email_query_bot.py --loop` 进程。
- 仓库仅在历史工单和本下线工单中保留邮件查询说明。
