# 工单：修补 onboarding 第二阶段审计发现的一致性问题

- **日期**：2026-08-08
- **状态**：已完成
- **范围**：`docs/00-START-HERE.md`；不改业务数据、运行代码或服务
- **theme**：文档一致性

## 现象

入口文档已更新到现行能力，但仍有四处细节不一致：生命周期图漏前两态和 `delivered`，代码地图仍为旧扁列表，Q5 未说明 closeout 的双调用边界，九三综合账号未在子系统表点明；自测标题也仍写 5 题而正文已有 7 题。

## 修复

- 将生命周期图与代码 8 态对齐，并保留朝钢短路规则。
- 以按子系统索引替换旧代码扁列表。
- 精确说明 `text-watch` 与 `status-sync --closeout` 的加载/重启关系。
- 在九三循环追踪行记录 `jz-port-super` 综合账号。

## 验收

- 文档状态、代码入口和 launchd 运行方式一致。
- Markdown 检查通过；无运行时代码变化。

## 结案

- 已补全 §3、§5 的 8 态表述，包含 `delivered` 与朝钢短路规则。
- 已将 §8 代码地图改为按子系统索引，修正 Q5 的双调用边界和自测题数量。
- 已注明九三四探针使用 `jz-port-super` 综合账号。
- 验证：`git diff --check`；相关生命周期、状态同步与九三循环测试 `27 passed`。
