# 工单：中唐宝丽 lot07 汽运改铁路手工建批

## 背景

宝丽第七票原为汽运清底计划，2026-08-03 收到“汽运返库剩余计划全部改为铁路返库”的通知。系统从原通知自动生成了未编号批次，并误沿用了汽运清底 `11,169` 吨，不能作为铁路发运归属。

## 已确认业务口径

- 项目：中唐特钢
- 船名：宝丽
- 批次：lot07
- 货物品名：麦克粉
- 发运方式：铁路
- 到站：汐子
- 放货量：9,000 吨
- 计划号：`90260800002`
- 合同号：`ZLDSZT-2026072801`
- 来源：2026-08-03 汽运计划改铁路的通知；原汽运 `11,169` 吨作为来源事实保留在原始 JSON，不作为铁路 lot07 放货量。

## 处理方案

1. 备份 `sop_agent.db`。
2. 将既有未编号宝丽批次就地校正为 `lot07`，不新建重复批次。
3. 更新数量、日期、计划号、合同号、货物品名和状态，并建立 active 的铁路匹配规则。
4. 校验中唐宝丽仅有一个 `lot07`、批次字段齐全、匹配规则 active；本次尚无铁路实发车，不触发发运链或外部发送。

## 验收

- `release_batches` 中宝丽 lot07 唯一且为 `loading`。
- `batch_quantity=9000`、计划号和合同号准确。
- active `release_dispatch_match_rules` 可将后续宝丽/汐子/铁矿铁路记录归属 lot07。

## 执行结果（2026-08-03）

- 已备份数据库：`data/backups/sop_agent_before_baoli_lot07_20260803_162110.sqlite3`。
- 已将原自动生成的未编号批次就地修正为 `lot07`；未新建重复批次。
- 已写入 `batch_date=2026-08-03`、`batch_quantity=9000`、计划号 `90260800002`、合同号 `ZLDSZT-2026072801`、麦克粉、铁路、汐子。
- 原通知中的汽运清底 `11,169` 吨保留在 `source_json`，并增加了可追溯的人工业务覆盖说明；不再用于本铁路 lot07。
- 批次当前 `loading`，匹配规则为 `active`、优先级 `700`。该批次尚无车记录，本次未触发发运 Excel、微信发送或收货人系统上传。
