# 工单: 九三和谐1七道历史补录

## 背景

和谐1 历史费用闭环已能生成, 但历史作业道线未完整落库:

1. 散粮 `bulk_loading_notice_wagon.track` 仍为占位值 `和谐1`。
2. 集装箱 `wagon_container_shipments.loading_line` 为空。

用户已确认该船历史散粮车和集装箱均按 `七道` 处理, 本次只做数据补录, 不调整计费规则。

## 目标

1. 将和谐1散粮历史记录统一补录为 `七道`。
2. 将和谐1集装箱历史记录统一补录为 `七道`。
3. 顺序重算 route A / route C 费用批次与 ship 对账, 让补录后的字段进入费用事实层与单证路径。

## 实施

1. 增加一次性可复跑补录脚本。
2. 更新 `bulk_loading_notice_wagon.track`。
3. 更新 `wagon_container_shipments.loading_line`。
4. 重新生成和谐1集装箱费用、散粮费用、船级对账。

## 验证

1. 散粮批次 `fee_batch.yard_line` 应为 `七道`。
2. 现场确认单文件名不再显示 `待补录`。
3. 集装箱批次不再出现 `missing_lines`。
