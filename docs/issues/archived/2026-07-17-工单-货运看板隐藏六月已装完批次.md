# 工单：货运看板隐藏六月已装完批次

- 日期：2026-07-17
- 类型：看板展示优化
- 范围：`scripts/cli_dashboard.py`

状态：已处理

## 背景

货运看板当前展示所有活跃生命周期状态，包含 `all_loaded`。2026 年 6 月已经人工确认装完的批次继续占用每个项目最多 8 条的展示位，挤压当前待处理批次。

## 规则

- 仅隐藏 `batch_date`（为空时回退 `notice_date`）在 `2026-06`、且 `dispatch_status='all_loaded'` 的批次。
- 保留 6 月的 `pending_freight`、`enriched`、`loading`、`tracking`、`delivered`。
- 保留其他月份的 `all_loaded`，以免掩盖当前在途或等待到货的批次。
- 不修改数据库状态、匹配规则或发运事实，仅影响终端看板显示。

## 验收

- [x] 2026-06 的 `all_loaded` 批次不再显示。
- [x] 6 月未装完批次与非 6 月的 `all_loaded` 批次仍显示（`test_dashboard_hides_only_june_all_loaded_batches`）。
- [x] 看板 tmux 进程已重载新代码（`cli_dashboard.py` 新进程 PID `64110`）。
