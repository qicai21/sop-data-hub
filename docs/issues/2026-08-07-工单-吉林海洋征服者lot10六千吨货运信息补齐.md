# 工单：吉林金钢海洋征服者 lot10 六千吨货运信息补齐

## 背景

`海洋征服者 lot10` 由 2026-08-07 第十次出港计划创建：四平铁路、6,000 吨。批次本身数量正确，但随后人工转述的货运信息误写为 3,000 吨，导致不能直接按错误口径补全。

## 用户最终确认（首次）

- 批次：第十次下达计划，lot10。
- 目的地：四平。
- 放货量：6,000 吨。
- 货物品名：印度粉。
- 入场合同号：`NJQH-20260806-02`。
- 订单标识：`CGR20260807145113`。

lot08、lot09 均为乌兰浩特（乌钢）放货，不纳入吉林金钢四平 SOP 批次及其自动匹配池。

## 处理与验收

1. 备份 `data/sop_agent.db` 后，就地更新既有 lot10；不得新建重复批次。
2. lot10 保持 6,000 吨，补齐货物品名、合同号、订单标识，并由 `pending_freight` 推进至 `enriched`。
3. 保持 lot10 四平铁路集装箱匹配规则为 active，优先级高于 lot06、lot07。
4. 验证 lot08、lot09 没有错误进入四平项目批次或匹配规则。

## 执行结果（2026-08-07）

- 已备份数据库：`data/backups/sop_agent_before_jg_ocean_lot10_freight_20260807_165053.sqlite3`。
- 已就地更新既有 lot10：四平、印度粉、6,000 吨、合同 `NJQH-20260806-02`、订单 `CGR20260807145113`。
- 批次状态已从 `pending_freight` 推进为 `enriched`，剩余量为 6,000 吨。
- 已刷新运行时匹配规则；lot10 规则为 `active`、优先级 1000。lot08、lot09 未进入吉林金钢四平批次或匹配规则。
- lot10 尚无发运记录；本次未生成 Excel、未发送微信、未上传收货人系统。

## 追加确认：拆分第十次计划（2026-08-07）

原始第十次计划仍为四平 6,000 吨，但业务上须拆为两个独立子批次：

- `lot10_a`：3,000 吨，印度粉，合同 `NJQH-20260806-02`，订单 `CGR20260807145113`，可发运。
- `lot10_b`：3,000 吨，四平；货运信息待补，保持 `pending_freight`。

系统原有顺序优先级仅识别纯数字 lot，新增 `lot10_a` / `lot10_b` 回归测试，保证 a 的优先级为 1000、b 为 1001，自动匹配必须先填满 a。

## 拆分执行结果（2026-08-07）

- 已备份数据库：`data/backups/sop_agent_before_jg_ocean_lot10_split_20260807_170211.sqlite3`。
- 原 lot10 已转换为 `lot10_a`：四平 3,000 吨、印度粉、合同 `NJQH-20260806-02`、订单 `CGR20260807145113`，状态 `enriched`。
- 已新增 `lot10_b`：四平 3,000 吨、状态 `pending_freight`，未填写货物品名、合同或订单。
- 两个子批次均保留第十次 6,000 吨原始出港计划 JSON，并写入人工拆分审计说明。
- 匹配规则已刷新：`lot10_a=1000`、`lot10_b=1001`；子批次内先 a 后 b。
- 回归测试：`tests/test_release_dispatch_priority.py` 与 `tests/test_match_release_batch.py` 共 11 项通过。

### 后续阻塞项

只读演练发现海洋征服者 `lot06` 仍为 `loading`，且已归属 183 箱、回算 5,907.24 吨，明显超过其 3,000 吨计划。因此全船的下一票自动匹配仍会先落到 lot06（优先级 600），不能以本次拆分直接绕过。该历史批次须先单独核对归属并封口或修正，不能在本工单中猜测处理。
