# 需求:九三大豆货票「自动对账(审查→更正)」—— 扫群/文件夹 → 比对 → 自愈分船

- **提出日期**:2026-06-27
- **类型**:功能开发(自动化 + 自愈)+ 关联缺陷修复
- **提出场景**:用户曾要求做"对账"功能(去九三大豆发运群/铁晟业务数据统计群查大豆发运文件并对账)。现核实**未实现**;由此诚信 90 张集装箱货票被错挂和谐1。
- **用户口径**:**数据先不手动补**,等本功能做完由它自动更正。

---

## ✅ 已修(2026-06-28 下午):reconcile 与 sync 打架 —— 改台账不改 wagon 表,定时已重开

**修法落地(按下方原 4 步「修法」)**:
1. **reconcile reroute/gate 改写台账**:`ReconcileSpec.persist_to_ledger(hub, rail, routes, log)`(base no-op);
   `JiusanContainerSpec` 写 `container_loading_notice`(id=sha1(ydid\|box),含 hph/car_no,source_ref='reconcile'),
   `JiusanBulkSpec` 写 `bulk_loading_notice_wagon`(有真通知单行→只改 ship_name,否则插 track='reconcile:<ydid>' 行)。
   `correct.reroute_mismatch` / `gate_finished_ships` **先写台账再改 wagon 表**(都接 `rail` 参数)。sync 按台账就不再 revert。
2. **sync 兜底船 → 当前活跃船**:`sync_jiusan_harmony_wagons.resolve_default_ship()` / `sync_jiusan_bulk_wagons.resolve_fallback_ship()`
   取唯一 `dispatch_status='loading'` 的 lot01/lot02 船(0 或多个 → 退回和谐1)。和谐1 发完后新箱不再默认堆它。
3. **sync 改 batch_id 时清 reconciled_at**:两个 sync 的 reroute 分支 UPDATE 一并 `reconciled_at=NULL/reconcile_source_ref=NULL`;
   一次性清掉现存 **180 箱**挂错船却标已核对的脏标记。
4. **验证闭环**:清脏标记 → `run_reconcile --apply`(reroute 180 箱→诚信、完成闸移 100 今日新箱→诚信(active)、写台账 280 条)
   → 跑 `sync_jiusan_all` **重路由纠正 0 box**(不再 revert)→ 二次 sync 仍 0 churn → reconcile mismatch=0。
   **结果**:和谐1 lot01=1709(干净) / 诚信 lot01=351(71 文件 + 180 纠正 + 100 今日新货待源)。
   单测 +1(`test_reroute_writes_ledger_and_clears_reconciled`),全套 **413 passed**。launchd `jiusan-reconcile` 已 bootstrap 重开。

**诚信散粮 426 vs 425**:多的 1 车 = car 8107032 / ydid 202606TY5027700098 / hph GZDZW0496404,**6-24 19:49 真实 95306 货票(已交付,69t)**,
是诚信循环车的最新一趟(其 6-19/6-22 趟在文件内),只是港方文件(6-20 快照 v5)还没收录。**非重复、非错挂、非文件漏**,
DB=426 正确;港方更新文件后 reconcile 自动标 OK。无需补数据。(原工单猜「循环重票边界」略偏——它是快照后新车,不是重票。)

---

## 🔴 (历史·已解决)关键发现(2026-06-28):reconcile 与 sync 打架 —— 待修,reconcile 定时已暂停

**现象**:reconcile reroute 把 90 货票归回诚信(13:09 标已核对),但 jiusan-sync daemon 13:33 又按 DEFAULT_SHIP 把它们写回和谐1(实证:某 6-27 箱 reconciled_at=13:09 < updated_at=13:33,source=`auto_jiusan_sync`),且 reconciled_at 没清 → "挂和谐1 却标已核对"卡死。每 3 小时反复。

**根因**:`sync_jiusan_harmony_wagons.py` **按 `container_loading_notice` 台账路由**(命中台账→该船,未命中→DEFAULT_SHIP=和谐1)。我的引擎 reconcile **直接改 `wagon_container_shipments`、没更新台账** → sync 一跑就 revert。工单原话「**更正台账→触发sync重路由**」才是正解,我改错了地方。

**修法(下次专项)**:
1. **reconcile 的 reroute/gate 改成写台账**(container→`container_loading_notice`、bulk→`bulk_loading_notice_wagon`),即 spec 加 `persist_to_ledger(key, ship)`;sync 按台账就不再 revert。(原 kernel `reconcile_jiusan_containers.py` 就是写台账的,我新引擎绕过了它。)
2. **sync DEFAULT_SHIP → 当前活跃船**(排除发完的船),配合完成闸,减少"新箱先堆和谐1再被纠"的窗口。
3. **sync 改 wagon.batch_id 时清 reconciled_at**(归属变了就该重核),或 reconcile 以台账为准重判。
4. 修后:清理现存"挂错船却标已核对"的脏 reconciled_at,重跑对账,再开定时。

**当前状态**:reconcile 定时已 bootout 暂停;wagon 表里诚信的 90 货票暂被 sync 拉回和谐1(数据没丢,台账打通后自动归位)。

## ★ 实现进度

- ✅ **阶段1 审查(只读)** 2026-06-28:`scripts/jiusan_reconcile_audit.py`。自动扫 WeChat `msg/file/<月>/` → 各船**最新版**『大豆X货票.xlsx』(读 `新台子`+`散粮车` 两 sheet)→ 按货票号(GZD)建应船 → 逐条比对实船。**纯只读**,复用 reconcile 内核。**照出三类不符**:
  - **① 集装箱 应≠实**:应诚信/实和谐1 = 180箱/90货票(=本工单 90 张,DEFAULT_SHIP 污染)。
  - **② 集装箱 无权威覆盖**:100箱(50货票)实挂和谐1、**日期 6-28**、货票号不在任何文件 + `wagon_container_shipments.hph 列为空`——**今天新到的箱,sync 默认堆和谐1 且没桥接货票号**(文件是 6-27 前快照,还没这批)。
  - **③ 散粮 无权威覆盖**:100车 实挂和谐1(6-10/6-12,文件散粮是 6-14 起,不含)+ 1车 诚信(6-24,循环重票边界)。
  - DB hph 列空时靠 ydid→rail 桥接补判;只对**有文件的船**报无权威(玛格丽特/昆娜等无文件历史船跳过不误判);GZDJW0496546 box 级例外排除。
- ✅ **通用对账框架**(2026-06-28,commit 1e4e4da/6819d59/156e1b9):重构成可复用引擎,不再是九三一次性脚本。
  - **三方模型**:95306(全集/真实性)× 额外源(归属)× DB(现状),归属锚=`release_batch_id`(非船名,船跨项目/lot复用)。
  - **引擎** `src/sop_hub/reconcile/engine.py`:五分类真值表(ok/mismatch/missing/new_unattributed/phantom)。每项目一个 `ReconcileSpec`(universe/db_rows/source_batch/backfill);九三是第一个实例(集装箱lot01/散粮lot02)。
  - **核对完毕状态**:`reconciled_at`/`reconcile_source_ref` 两列;三方剔除已核对→每日只算未核对+新增+源更新过的(幂等);历史船 grandfather 封存。
  - **阶段2 更正(--apply)**:自动 reroute 错挂(改归源batch+重算聚合,可逆)+ spec backfill(集装箱补hph);**phantom(删)/new(分船)不自动做,只告警**(用户定的稳法)。**逐笔日志**:`reconcile_action_log`表 + `runtime/reconcile.log`文件。
  - **实测自愈**:九三集装箱 reroute 90货票/180箱(诚信污染归回诚信)+ backfill 100箱hph,错挂清零;散粮 phantom 100(双写)+ new 只告警。
- ✅ **完成闸**(commit d84e036):发完的船(标 confirmed_received)排除出默认;新货挂在发完船上 → 移到当前唯一活跃船,`gate_move` 日志。实测和谐1 标发完后,6-28 的 50 票从和谐1 移到诚信。
- ✅ **阶段3 调度**(commit, plist `deploy/launchd/`):launchd **周日~五 22:00** 跑 `run_reconcile --apply`,已加载+kickstart 验证。每日干净(只剩真新货等源)。
- ✅ **一次性清理 + --delete-phantom**(commit 3c5d301):双写根因=手工 seed(`manual_harmony1_*`,非 sync,散粮 sync 本就按整车过滤);加人工触发 `--delete-phantom` 删那 100;清后散粮和谐1=320/诚信=426 与 95306+Excel 完全一致。
- ✅ **引擎单测**(commit,5 case):五分类真值表 + 已核对剔除 + 元组key一票两船。
- ⬜ **closeout 自动标发完**:`完成闸`靠船标 confirmed_received,现 和谐1 是手工标的(它卡在陈旧 loading,昆娜/玛格丽特却已 confirmed)。要让完成闸长期自动,需 lifecycle 在船发完时自动标 —— **待查为何 和谐1 没被自动 closeout**。
- ⬜ **sync DEFAULT_SHIP 入库端闸**(治本,reconcile 完成闸是兜底):`sync_jiusan_harmony_wagons.py:35 DEFAULT_SHIP` 入库就别堆发完的船。

**九三对账框架已完整上线**:审查→更正(reroute/backfill/完成闸)→删(人工)→标核对完毕→每日22点幂等自愈,逐笔日志。其他项目复用只写 ReconcileSpec。

## 一、现状核实(2026-06-27)

**对账"逻辑"有(手动),"自动"没有**:
| 件 | 现状 |
|---|---|
| `scripts/reconcile_jiusan_containers.py` | 集装箱拆船对账,**手动** `--file <xlsx> --ship` |
| `scripts/ingest_bulk_loading_notice.py` | 散粮拆船 ingest,**手动**(位置参数 `xlsx`) |
| `sync_jiusan_all`(launchd jiusan-sync) | 只**按台账路由**,不更新台账;台账没新货票→新箱默认堆和谐1 |
| `jiusan-bulk-report-ingest` daemon | 跑 `jiusan_bulk_report_ingest.py`(散粮**日报**),**非**货票对账 |
| 自动扫群/文件夹→发现新货票→reconcile→重同步 | **不存在** |

**源文件其实自动落地了**:微信文件夹 `…/com.tencent.xinWeChat/.../msg/file/2026-06/`(WeChat 设定**按月**滚动)已有 `26.6.20大豆 诚信 货票(2)…(5).xlsx`、`大豆 和谐1 货票.xlsx`、`诚信 散粮车6.23/6.24.xlsx` 等——**港方更新会叠版本((2)~(5)),需取最新版**。但无人消费。
- ⚠️ `铁晟业务数据统计群` 在 events 管道里**不存在**(只有"铁晟业务工作群")——群名待用户确认/补监听。九三大豆发运群 events 有。

## 二、由此产生的实际错账(审查证据)

以用户提供的港方权威货票 xlsx(和谐1 / 诚信)逐货票比对当前 DB:
```
                  xlsx(权威)      DB           不符
和谐1 集装箱        855货票          855          ✓(含1张一车两船 GZDJW0496546 已正确拆:
                                                    TBJU1512630→诚信 / TBJU5804218→和谐1)
和谐1 散粮          320              320          ✓
诚信 散粮          425              ~426         ✓(差1为循环重票边界)
诚信 集装箱        125货票          35对+90错     ✗ 90张错挂和谐1
```
**错账=诚信 6/26(40张)+6/27(50张)集装箱货票全被挂到和谐1**(DB 和谐1 lot01 batch 实 945货票/1889箱 = 855真+90污染;存字段 855 还陈旧)。
**根因**:`sync_jiusan_harmony_wagons.py` 的 `DEFAULT_SHIP="和谐1"` 兜底——台账(`container_loading_notice`)只 reconcile 到 6/25,6/26/6/27 诚信新趟没入台账 → 未命中 → 默认堆和谐1。**与散粮回归(`2026-06-20` 工单)、集装箱拆船(`2026-06-23` 工单)同源**:台账不及时更新 + 默认落和谐1。

## 三、期望功能:自动对账(审查 → 更正),自愈

**目标**:把"港方给货票 → 手动 reconcile"变成**定时自动**:发现新货票 → 审查不符 → 更正台账 → 重同步 → 看板自愈。**数据不需人工补**。

1. **源采集**:监听 `九三大豆发运群` + `铁晟业务数据统计群`(确认群名);或直接扫 WeChat `msg/file/<YYYY-MM>/` 文件夹(按月滚动要跨月兜底)。识别大豆货票文件(和谐1/诚信、集装箱"新台子"sheet/散粮"散粮车"sheet),**多版本取最新**((5)>(4)>…/按 mtime)。
2. **审查(audit)**:对每船最新货票 xlsx,按 **hph(货票号)+ box_no** 逐箱比对 DB 现挂船;输出不符清单(应船 vs 实船、张数、日期)。**只读、可单独跑出报告**。
3. **更正(correct)**:把不符项写回台账(`container_loading_notice` / `bulk_loading_notice_wagon`)→ 触发 sync 重路由 → 各船 batch 重算聚合。**幂等、自愈**(再跑不符=0)。一车两船例外按 box 级保留(如 GZDJW0496546)。
4. **调度**:新增 launchd(或并入 jiusan-sync 链);每次 sync 前先跑对账更新台账,再路由。
5. **兜底口径修复(治本,呼应散粮/集装箱工单)**:`DEFAULT_SHIP` 不写死和谐1 —— 和谐1 集装箱/散粮发完后,未登记新箱**不再默认堆它**(加完成闸/默认落当前活跃船/挂 pending+告警)。否则每趟新船箱在对账跑到之前仍会瞬时漏挂(自愈前的窗口)。
6. **复用**:`reconcile_jiusan_containers.py`(集装箱)、`ingest_bulk_loading_notice.py`(散粮)做内核,套一层"扫文件夹→选最新→调用→重同步"的自动驱动。

## 四、验收
- 把诚信最新货票放进文件夹 → 自动对账后:诚信 lot01 = 125货票/250箱、和谐1 lot01 = 干净 855/1709,聚合字段同步刷新;诚信散粮独立正确。
- audit 报告能列出"应船 vs 实船"不符;correct 后再跑不符=0;二次跑幂等。
- 一车两船例外(GZDJW0496546)box 级保持。
- 新趟未对账期间不污染已发完的船(完成闸生效)。

## 五、关联
- 同源工单:`2026-06-20-散粮sync按船拆分.md`(回归·默认落和谐1)、`2026-06-23-集装箱sync拆船+循环效率统计+在装车显示.md`(回归段)
- 复用:`scripts/reconcile_jiusan_containers.py`、`scripts/ingest_bulk_loading_notice.py`、表 `container_loading_notice` / `bulk_loading_notice_wagon`
- 待改:`scripts/sync_jiusan_harmony_wagons.py`(`DEFAULT_SHIP`)、`scripts/sync_jiusan_bulk_wagons.py`、`scripts/sync_jiusan_all.py`
- 源:WeChat `msg/file/<YYYY-MM>/`(按月)、群 `九三大豆发运群[GROUP093]` / `铁晟业务数据统计群`(待确认)
- 记忆:[[jiusan-bulk-ship-split-rule]]、[[jiusan-container-ship-split]]、[[wx-group-search-codes]]
- **数据修复:暂缓**(用户定),待本功能上线后自动更正那 90 张。
