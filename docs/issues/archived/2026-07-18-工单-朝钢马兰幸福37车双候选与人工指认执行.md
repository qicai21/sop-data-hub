# 工单：朝钢马兰幸福 37 车双候选与人工指认执行

- 日期：2026-07-18
- 类型：检装车候选归并 / 同船多 lot 分配
- 项目：朝阳钢铁

状态：已处理

## 事实

`铁晟业务工作群` 2026-07-17 20:33 的检装车通知单（`wx_2026-07_1411`）记录 45 节：

- 实装：37 车；
- 排车：8 车，不应入库；
- 实装车的 95306 制票窗口为 `20:43:03–20:49:42`，均为高桥镇→朝阳西铁矿粉；
- `马兰幸福 lot02` 仍为 loading，`lot03` 已建批并补齐货运信息，导致普通“船名+到站+货物”匹配得到多个开放批次。用户确认本次 37 车应归属 `lot02`。

系统还由文本触发器建出 `wx_2026-07_1412` 的 37 车合成候选，与真实图候选属于同一物理发运列。

## 处理

1. 人工指认真实图候选 `50418df...` 归属 `马兰幸福 lot02`（`9cd643...`）。
2. 执行器仅在候选带有审计化 `_manual_assignment`、且批次仍为同项目/船名/到站的开放批次时，采纳该指认；不再因同船多 lot 而重新落入 `multiple_candidates`。
3. 以真实图为唯一事件执行 37 车标准链：95306 入库、发运 Excel、鞍钢上传及反查；同车集合的合成候选由真实图自动 supersede。
4. 合成候选即使链路失败后 `reason` 被覆盖，也按 `payload_json.source='95306_synthesized'` 识别并淘汰，避免残留 pending review。

## 验收

- [x] 37 条新 ydid 仅归入 `马兰幸福 lot02`（批次 `9cd643...`）。
- [x] 原图候选 `50418df...` 为 `matched`；同车集合的合成候选 `6dec8e...` 为 `superseded`；当前 `pending_review=0`。
- [x] 已生成并发送 [马兰幸福发运_2026-07-17_37车.xlsx](/Users/qicai21/Documents/bussiness-artifacts/wechat_images/business/projects/chaoyang_steel/朝阳西/马兰幸福/lot02/excel/2026-07-17/马兰幸福发运_2026-07-17_37车.xlsx)，鞍钢门户上传及反查均为 `37/37`。
- [x] 原工作流任务 `690` 及其 inbox 均已回写为 `succeeded`。
